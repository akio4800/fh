select * from ((persoenlicherassistent pa inner join person p on (p.personid = pa.paid)) inner join Adresse ad on (ad.adressid=p.adressid));

select * from ((persoenlicherassistent pa inner join person p on (p.personid = pa.paid)) inner join Adresse ad on (ad.adressid=p.adressid));